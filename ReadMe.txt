- Created an API which can be consumed by an Angular Application or a windows service via which the report can be generated by fetching the file from the mentioned path in the appConfig.
- Made use of parallel calls to support high amount of data
- Used singleton design pattern to inject depedency of the utlity class which will be used throughout the life cycle of application
- returning the data in json format with all the required properties for the report